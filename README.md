# Elon-Inspired Strategic Dialogue AI

マスク的思考スタイル（第一原理思考、高リスク・高インパクト戦略、高速意思決定）で回答するAI対話システム。

![Version](https://img.shields.io/badge/version-1.0.0-blue)
![Docker](https://img.shields.io/badge/docker-ready-green)

## 🚀 特徴

- **第一原理思考モード**: 問題を根本から分解し、ゼロベースで再構築
- **戦略シミュレーション**: 高インパクト・高リスク戦略の探索
- **高速応答**: 60秒以内の応答保証
- **ダークモードUI**: プレミアムなチャットインターフェース

## 📋 必要条件

- Docker & Docker Compose
- OpenAI API キー

## 🛠️ セットアップ

### 1. 環境変数の設定

```bash
cp .env.example .env
```

`.env` ファイルを編集し、OpenAI API キーを設定:

```
OPENAI_API_KEY=your_openai_api_key_here
```

### 2. Docker コンテナの起動

```bash
docker-compose up -d --build
```

### 3. アクセス

- **フロントエンド**: http://localhost:3000
- **バックエンドAPI**: http://localhost:8000
- **API ドキュメント**: http://localhost:8000/docs

## 📁 プロジェクト構成

```
elon-ai/
├── docker-compose.yml      # Docker構成
├── backend/                # FastAPI バックエンド
│   ├── main.py            # アプリケーションエントリ
│   ├── config.py          # 設定管理
│   ├── routers/           # APIルーター
│   │   └── chat.py        # チャットエンドポイント
│   └── services/          # ビジネスロジック
│       ├── thinking_engine.py  # 思考エンジン
│       └── openai_client.py    # OpenAI統合
├── frontend/              # Next.js フロントエンド
│   └── app/               # App Router
│       ├── layout.tsx     # ルートレイアウト
│       ├── page.tsx       # チャットUI
│       └── globals.css    # グローバルスタイル
└── .devcontainer/         # VS Code Dev Container
```

## 🔧 開発

### コンテナログの確認

```bash
docker-compose logs -f
```

### コンテナの停止

```bash
docker-compose down
```

### コンテナの再ビルド

```bash
docker-compose up -d --build
```

## 📡 API エンドポイント

| メソッド | パス | 説明 |
|---------|------|------|
| GET | `/health` | ヘルスチェック |
| POST | `/api/chat` | メイン対話エンドポイント |
| POST | `/api/chat/stream` | ストリーミング対話 |
| GET | `/api/modes` | 利用可能な思考モード |

### リクエスト例

```bash
curl -X POST http://localhost:8000/api/chat \
  -H "Content-Type: application/json" \
  -d '{
    "message": "電気自動車産業の未来は？",
    "mode": "first_principles"
  }'
```

## 📜 ライセンス

MIT License
